# Configuração no /etc/nginx/nginx.conf
# Bloco stream { } fica FORA do bloco http { }

user www-data;
worker_processes auto;
pid /run/nginx.pid;
include /etc/nginx/modules-enabled/*.conf;

events {
    worker_connections 16384;
    multi_accept on;
}

http {
    ##
    # Basic Settings
    ##

    # ATENÇÃO À ORDEM: tem que vir ANTES do primeiro `map` deste bloco (o
    # upstream_http.conf logo abaixo já traz um). A diretiva `map` preenche
    # esses dois com o default quando ainda estão vazios, e a partir daí o
    # nginx recusa qualquer map_hash_bucket_size como "directive is duplicate".
    # Precisamos de bucket maior por causa do certs_map.conf, que tem ~122
    # entradas com nomes de domínio longos (o padrão 64 estoura).
    map_hash_bucket_size 256;
    map_hash_max_size    4096;

    include /etc/nginx/upstream_http.conf;
    
    sendfile on;
    tcp_nopush on;
    types_hash_max_size 2048;
    # server_tokens off;

    # server_names_hash_bucket_size 64;
    # server_name_in_redirect off;

    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    ##
    # SSL Settings
    ##

    ssl_protocols TLSv1 TLSv1.1 TLSv1.2 TLSv1.3; # Dropping SSLv3, ref: POODLE
    ssl_prefer_server_ciphers on;

    ##
    # Logging Settings
    ##

    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    ##
    # Log estruturado JSON enviado via syslog para o Vector (192.168.2.50:5140).
    # Alimenta a tabela ClickHouse netmon.nginx_access e os dashboards Grafana.
    ##
    log_format http_json escape=json '{'
        '"ts":"$time_iso8601",'
        '"src":"http",'
        '"host":"$host",'
        '"client":"$remote_addr",'
        '"cf_ip":"$http_cf_connecting_ip",'
        '"xff":"$http_x_forwarded_for",'
        '"backend":"$backend_name_http",'
        '"upstream":"$upstream_addr",'
        '"upstream_status":"$upstream_status",'
        '"method":"$request_method",'
        '"uri":"$request_uri",'
        '"status":$status,'
        '"size":$body_bytes_sent,'
        '"req_time":$request_time,'
        '"upstream_time":"$upstream_response_time",'
        '"referer":"$http_referer",'
        '"ua":"$http_user_agent",'
        '"ssl_proto":"$ssl_protocol",'
        '"scheme":"$scheme",'
        '"req_len":$request_length'
    '}';

    ##
    # Gzip Settings
    ##

    gzip on;

    # gzip_vary on;
    # gzip_proxied any;
    # gzip_comp_level 6;
    # gzip_buffers 16 8k;
    # gzip_http_version 1.1;
    # gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript;

    ##
    # Virtual Host Configs
    ##

    include /etc/nginx/conf.d/*.conf;
    # include /etc/nginx/sites-enabled/*;
    log_format http_detailed '[$time_local] '
                             'Host: $host | '
                             'Client: $remote_addr | '
                             'CF-IP: $http_cf_connecting_ip | '
                             'X-Forwarded-For: $http_x_forwarded_for | '
                             'Backend: $backend_name_http | '
                             'Method: $request_method | '
                             'URI: $request_uri | '
                             'Status: $status | '
                             'Size: $body_bytes_sent bytes | '
                             'Referer: "$http_referer" | '
                             'User-Agent: "$http_user_agent" | '
                             'Request-Time: ${request_time}s | '
                             'Upstream: $upstream_addr | '
                             'Upstream-Time: ${upstream_response_time}s';
    server {
        listen 127.0.0.1:8080;

        access_log /var/log/nginx/http_router.access.log http_detailed;
        # access_log syslog:server=192.168.2.50:5140,tag=nginx_http,severity=info http_json;
        error_log /var/log/nginx/http_router.error.log;
	location / {
            proxy_pass http://$backend_name_http;

            # Headers essenciais
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto https;  # Cloudflare usa HTTPS
            proxy_set_header X-Forwarded-Host $host;

            # Headers do Cloudflare
            proxy_set_header CF-Connecting-IP $http_cf_connecting_ip;
            proxy_set_header CF-Ray $http_cf_ray;

            # Timeouts
            proxy_connect_timeout 600;
            proxy_send_timeout 600;
            proxy_read_timeout 600;
            send_timeout 600;

            # WebSocket support
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
	}

    }

    ###########################################################################
    # PÁGINA DE BLOQUEIO — servida a quem NÃO está na VPN (porta 443)
    #
    # A 443 é TLS passthrough (ssl_preread), então o bloco stream não consegue
    # escrever uma resposta HTTP: ele só encaminha bytes cifrados. Para mostrar
    # a página de aviso é preciso TERMINAR TLS, e para isso é preciso ter o
    # certificado daquele hostname — senão o browser barra no aviso de
    # certificado e o usuário nunca lê a mensagem.
    #
    # Os certificados são espelhados do Traefik (.116) por
    # /usr/local/bin/sync-traefik-certs.sh, que também gera o certs_map.conf.
    # Assim não existe um segundo cliente ACME brigando pelo desafio HTTP-01
    # nem consumindo rate limit do Let's Encrypt.
    ###########################################################################

    include /etc/nginx/certs_map.conf;   # map $ssl_server_name -> $blockpage_cert

    server {
        listen 127.0.0.1:8443 ssl http2;

        # Certificado escolhido por SNI. O mapa é gerado a partir dos arquivos
        # que existem de fato, então nunca aponta para um caminho inexistente
        # (o que derrubaria o handshake em vez de mostrar a página).
        ssl_certificate     $blockpage_cert.crt;
        ssl_certificate_key $blockpage_cert.key;
        ssl_protocols       TLSv1.2 TLSv1.3;
        ssl_session_cache   shared:BLOCKED:5m;

        access_log /var/log/nginx/blocked.access.log http_detailed;
        error_log  /var/log/nginx/blocked.error.log;

        # 403 em qualquer rota — inclusive chamadas de API, que assim recebem
        # um status inequívoco em vez de um erro de conexão sem explicação.
        error_page 403 /index.html;
        location / { return 403; }
        location = /index.html {
            internal;
            root /var/www/html/blocked;
            add_header Cache-Control "no-store" always;
        }
    }
}
stream {
    ###########################################################################
    # Formato de logs para stream (TCP/UDP - sem variáveis HTTP)
    ###########################################################################

    include /etc/nginx/upstream_https.conf;
    log_format stream_proxy '[$time_local] '
                            'Protocol: $protocol | '
                            'Client: $remote_addr:$remote_port | '
                            'Backend: $server_addr:$server_port | '
                            'Upstream: $upstream_addr | '
                            'Bytes Sent: $bytes_sent | '
                            'Bytes Received: $bytes_received | '
                            'Session Time: $session_time s | '
                            'DNS: $ssl_preread_server_name | '
                            'Status: $status';

    log_format stream_json escape=json '{'
        '"ts":"$time_iso8601",'
        '"src":"stream",'
        '"protocol":"$protocol",'
        '"client":"$remote_addr",'
        '"client_port":$remote_port,'
        '"backend_addr":"$server_addr",'
        '"backend_port":$server_port,'
        '"upstream":"$upstream_addr",'
        '"sni":"$ssl_preread_server_name",'
        '"bytes_sent":$bytes_sent,'
        '"bytes_recv":$bytes_received,'
        '"session_time":$session_time,'
        '"status":$status'
    '}';

    # Servidor HTTPS com TLS passthrough
    server {
        listen 443;
        listen [::]:443;

        access_log /var/log/nginx/stream_https.access.log stream_proxy;
        # access_log syslog:server=192.168.2.50:5140,tag=nginx_stream,severity=info stream_json;
        error_log /var/log/nginx/stream_https.error.log;

        proxy_pass $backend_name_https;
        ssl_preread on;
    }

    ###########################################################################
    # Porta 80 — TCP proxy com roteamento híbrido
    ###########################################################################

    include /etc/nginx/upstream_stream_80.conf;

    server {
        listen 80;
        listen [::]:80;

        access_log /var/log/nginx/stream_http.access.log stream_proxy;
        # access_log syslog:server=192.168.2.50:5140,tag=nginx_stream,severity=info stream_json;
        error_log /var/log/nginx/stream_http.error.log;

        proxy_pass $backend_name_80;
        ssl_preread on;
    }
}

